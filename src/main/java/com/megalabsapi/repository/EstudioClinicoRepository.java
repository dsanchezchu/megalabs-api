package com.megalabsapi.repository;

import com.megalabsapi.model.entity.EstudioClinico;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EstudioClinicoRepository extends JpaRepository<EstudioClinico, Long> {
}